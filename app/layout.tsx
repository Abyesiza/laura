import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "AURA MAGICAL CLEAN - Premium Liquid Soap",
  description: "Discover AURA MAGICAL CLEAN liquid soap - premium quality, gentle care, and magical cleanliness for your home.",
  keywords: ["liquid soap", "hand soap", "premium soap", "AURA", "gentle soap", "moisturizing soap"],
  authors: [{ name: "AURA MAGICAL CLEAN" }],
  icons: {
    icon: "/favicon.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
